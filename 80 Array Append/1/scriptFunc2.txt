CONTOH SCRIPT

@echo off
setlocal enabledelayedexpansion
goto :main

:main
setlocal

	call create_array array " " "SOMETIMES WE WILL NEVER KNOW THE VALUE OF A MOMENT"
	call array_append array " " "until it becomes memory"
	echo !array!
	echo !array_length!
	echo !array[0]!


endlocal
goto :eof
